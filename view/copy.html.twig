{{
    props({
        linkColor: t.string().isRequired(),
        text: t.string().isRequired(),
    })
}}

<a href="#"
   class="copy link-span-only {{ linkColor }}"
   data-clipboard-text="{{ text }}"
>
    <i class="fas fa-copy fa-sm fa-fw"></i>
    <span>{{ _('global', 'copy') }}</span>
</a>